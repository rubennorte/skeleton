
/**
 * <%= humanName %> model
 */

define([
  'skeleton/model'
], function(Model){
  
  var <%= className %> = Model.extend({

    // Add your default values, initialization, validation, etc. here

  });
  
  return <%= className %>;

});